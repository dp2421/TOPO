2월 8일
CMeshData* CMeshData::LoadFromFBX(const wstring& _strPath)
구조를 컨테이너 전체를 가져올수 있도록 수정
그러면 CMesh* pMesh = CMesh::CreateFromContainer(loader); 요기도 인덱스도 같이 인자로 받도록 수정
CMesh 구조 살펴보고 컨테이너 전체를 받을수있는건지? 아마안될거임 그럼 컨테이너 자료형으로 받아오고
CMeshData 이쪽은 어떻게 되는건지 살펴보기
그럼 이거 렌더링 쪽도 전부 수정해야 하는건가?? 정말 화나게한다


2월 9일
mesh, meshdata 전부 하나씩만 담을 수 있음..(당연함..)
오브젝트에 여러 개를 담을 수 있는지가 관건일지도
안되면 그냥 같은 레이어에 여러개 오브젝트를 추가해야 할 것 같은데 
흠
오브젝트도 배열로 관리하는 방법은?

애초에 모든 함수, 메쉬 등이 메쉬 하나만 관리할 수 있도록 되어있다..
MeshRender m_pMesh render 함수 보기(subset 인덱스 벗어나는건지 체크)

MeshData 에 벡터로 Mesh 여러갤 저장할 수 있는 멤버변수 추가: m_pMeshContainer
GetContainer 요런것들 for문 돌면 컨테이너 갯수만큼 불러오게 해야함

현재 MeshData.cpp 에 LoadFromFBX 함수에 메쉬 가져오는 쪽 그렇게 바꿔뒀으니, 참고해서 작성부탁
지금 뜨는 꼬리: 반복문돌면서 제일 마지막 메쉬만 불러오도록 되어있어서 꼬리만 뜨는 거


2월 12일
메쉬 벡터를 사용하도록 변경했는데 암것도 안뜬다...
분명 vecMesh 에 값을 넣어줬는데도 값이 없다고 뜸. 어디서 문제가 생긴거지??

2월 13일
뭔가 어딘가에서 vecMesh가 아니라 m_pMesh에 값을 넣어줬던 게 틀림없다
넣어주는 값만 vecMesh로 변경했을 때 아무것도 안 떠서 멘붕왔는데
렌더링 부분까지 싹다 반복문을 돌면서 vecMesh를 렌더링하도록 변경, m_pMesh 구조는 더이상 사용하지 않는다
렌더하는 함수쪽이나 MeshRender 클래스 뜯어보면 vecMesh로 전부 바꿔둠
파티클 사용하는 쪽이 아직 mesh를 사용하긴 하는데 이쪽은 아직 건드린 게 아니고 별 문제 없어서 일단 냅뒀다

이제 할일은 맵도 띄워보고 하는건데.. 맵 만들기 싫어 죽겠다
이래서 모델러를 쓰는 거구나 절실히 느끼는 요즘

2월 15일
며칠을 쉰거야?
보니까 역시 렌더함수 쪽에서 지금 렌더링 안 되는 오브젝트들이 렌더가 안 되는 거였음
Camera.cpp 에서 SortGameObject 함수 문젠데 렌더링 할 오브젝트를 뭐 조건에 따라 m_vecDeferred에 push_back 하는 부분에서
조건문을 전부 통과하는 대참사...
조건문을 살펴본 후 대체 뭐가 입력이 안된건지 봐야함

2월 16일
애니메이션이 없다는 이유로 뭔가가 값이 들어가지 않고있다..를 파악
Instantiate에서 메쉬와 머터리얼 값을 넣어주고 있기 때문에, 그쪽을 확인해 봤는데
메쉬 설정 중 애니메이션이 없으면 그 자리에서 함수를 리턴해버려서
밑에서 머터리얼 설정해주는 구문까지 도달하지 못하는 게 이유였다..
그래서 애니메이션이 없을 경우 리턴시키는 게 아니라 컨티뉴문으로 애니메이션 설정부분만 건너뛰도록 해줌
이제 잘될겨

